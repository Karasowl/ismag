---
import Base from '../layouts/Base.astro';
import SectionHeader from '../components/SectionHeader.astro';
import ArticleCard from '../components/ArticleCard.astro';
import { getCollection } from 'astro:content';

const articles = (await getCollection('articulos', ({ data }) => !data.draft && data.category === 'marketing-ia'))
  .sort((a, b) => b.data.date.getTime() - a.data.date.getTime());
---

<Base
  title="Marketing & IA"
  description="Artículos sobre marketing digital, inteligencia artificial y tendencias tecnológicas. Análisis prácticos desde la trinchera, no desde el hilo viral."
>
  <section class="max-w-content mx-auto px-4 sm:px-6 py-12">
    <SectionHeader
      title="Marketing & IA"
      subtitle="Análisis prácticos sobre marketing digital e inteligencia artificial. Sin hype, con resultados."
    />

    {articles.length > 0 ? (
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        {articles.map((article) => (
          <ArticleCard
            title={article.data.title}
            description={article.data.description}
            date={article.data.date}
            category={article.data.category}
            slug={article.id}
            image={article.data.image}
          />
        ))}
      </div>
    ) : (
      <p class="text-muted">Artículos próximamente.</p>
    )}
  </section>
</Base>
